from setuptools import setup, find_packages

setup(
    name="Flatten",
    version="1.0",
    packages=find_packages(),
    # entry_points={
    #     "console_scripts": [
    #         # "flatten = Flatten.main:main",
    #     ],
    # },
)
